<!DOCTYPE html>
<html>
<head>
    <title>Create Organization - Invoice Payment System</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <div class="container">
        <div class="form-header">
            <h1>Create New Organization</h1>
            <a href="/" class="btn back-btn">Back to Home</a>
        </div>

        <% if (locals.error) { %>
            <div class="error-message">
                <%= error %>
            </div>
        <% } %>

        <form action="/create-organization" method="POST" class="form-container">
            <div class="form-group">
                <label for="name">Organization Name:</label>
                <input 
                    type="text" 
                    id="name" 
                    name="name" 
                    required
                    value="<%= locals.formData ? formData.name : '' %>"
                    class="form-control"
                >
                <small>Organization name must be unique</small>
            </div>

            <button type="submit" class="btn submit-btn">Create Organization</button>
        </form>
    </div>
</body>
</html>